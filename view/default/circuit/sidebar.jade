.peek#sidebar
  .ui.vertical.pointing.menu
    a.icon.item(class=(($_G['basescript'] == 'circuit' && (CURMODULE == 'list' || CURMODULE == 'search')) ? 'active' : ''), href='/circuit')
      i.list.layout.icon
      | List
    a.icon.item(class=(($_G['basescript'] == 'circuit' && CURMODULE == 'upload') ? 'active' : ''), href='/circuit/upload')
      i.edit.icon
      | Upload
